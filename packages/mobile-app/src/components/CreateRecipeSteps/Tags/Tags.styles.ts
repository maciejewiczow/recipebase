import styled from 'styled-components/native';
import { StepHeaderWrapper as OriginalStepHeaderWrapper } from '../common.styles';

export const StepHeaderWrapper = styled(OriginalStepHeaderWrapper)`
    margin-bottom: 18px;
`;
