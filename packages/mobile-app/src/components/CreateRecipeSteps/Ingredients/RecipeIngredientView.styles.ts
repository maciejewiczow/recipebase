import { styled } from 'styled-components/native';
import { AddIngredientButton as OriginalAddIngredientButton } from './AddIngredientButton';

export const AddIngredientButton = styled(OriginalAddIngredientButton)`
    margin-left: 0;
    margin-right: 0;
`;
